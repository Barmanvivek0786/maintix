// AdMob/Watch Ad widget removed per product decision — file kept as empty placeholder
import 'package:flutter/material.dart';

class HomeAdButtonWidget extends StatelessWidget {
  const HomeAdButtonWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
